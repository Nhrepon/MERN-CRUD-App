import React from 'react';

const Loader = () => {
    return (
        <div className="container">
            <div className="loading"></div>
        </div>
    );
};

export default Loader;